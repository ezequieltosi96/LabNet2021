@model IEnumerable<Lab.LINQ.Entities.Customer>

@{
    ViewBag.Title = "GetWashingtonCustomers";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2 class="text-center" style="margin: 4rem;">Get Washington Customers</h2>

<table class="table table-striped table-hover text-center">
    <tr>
        <th class="text-center">
            @Html.DisplayNameFor(model => model.CompanyName)
        </th>
        <th class="text-center">
            @Html.DisplayNameFor(model => model.ContactName)
        </th>
        <th class="text-center">
            @Html.DisplayNameFor(model => model.ContactTitle)
        </th>
        <th class="text-center">
            @Html.DisplayNameFor(model => model.City)
        </th>
        <th class="text-center">
            @Html.DisplayNameFor(model => model.Region)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.CompanyName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ContactName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ContactTitle)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.City)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Region)
            </td>
            <td>
                @Html.ActionLink("Customer Details", "GetCustomer", new { id = item.CustomerID })
            </td>
        </tr>
    }

</table>
